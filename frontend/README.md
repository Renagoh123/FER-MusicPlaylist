# FER Music Playlist Frontend

Next.js frontend for the FER Music Playlist app.

## Setup

```sh
cd frontend
npm install
copy .env.example .env.local
npm run dev
```

The app runs at `http://localhost:3000`.

The backend should be running at `http://localhost:8000`.
